///This combined with the commented text in the index.html is a bad way of capturing event data! But might be usefull!
[JSInvokable]
public static void MouseMove(MouseEventArgs args)
{
    mouseX = args.ClientX;
    mouseY = args.ClientY;
